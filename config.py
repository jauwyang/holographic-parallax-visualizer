# Controls scale/size of screen
SCALE = 0.5  # (SCALE = 1 uses same resolution as image)

# Controls how sensitive parallax effect is to user movement
PARALLAX_SENSITIVITY = 32  # (lower val = higher sensitivity)

# Controls allowable direction to perform parallax effect/shift
X_TRANSFORM = True
Y_TRANSFORM = True

# Controls the depth ranges of each parallax layer
DIVISION_SIZE = 30  
